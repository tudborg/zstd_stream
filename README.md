# ZstdStream

Elixir Stream of bytes compress / decompress transforms using OTP28's zstd module 

## Installation

Probably something like this:

```elixir
def deps do
  [
    {:zstd_stream, "~> 0.1.0", git: "https://github.com/tudborg/zstd_stream.git", tag: "v0.1.0"}
  ]
end
```

